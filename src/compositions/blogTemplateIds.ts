// Extracted from app/dashboard/_components/BlogSceneThumbnail.tsx so the render
// bundle doesn't pull in @remotion/player. Keep in sync with that file.
export const BLOG_TEMPLATE_IDS = new Set([
  "default", "geometric-explainer", "nightfall", "gridcraft", "spotlight",
  "whiteboard", "newspaper", "matrix", "newscast", "mosaic", "blackswan", "bloomberg", "chronicle",
]);
